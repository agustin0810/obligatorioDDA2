
* Dashboard principal           DONE

* ABM Planes de viaje:
	-Destino (max 20), fecha, modalidad (area, maritima o terrestre), precio(USD),
	carrusel de fotos (opcional).

* ABM Clientes:
	-CI (max 8, min 7, no ., -), nombre (max 30), apellido (max 30), email (max 30),
	planes comprados (mensaje si no tiene).
	-Clientes standard y premium (más de 10 viajes, 20% descuento para siempre)

* Asociar cliente a plan de viaje (desde planes de viaje)

* Listar los planes para un cliente (desde planes, todos los attr)

* Listar el primer viaje que tendrá un cliente desp de fecha.

* Control fecha para los viajes

* Control de errores ingreso de datos